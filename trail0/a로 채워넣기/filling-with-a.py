# 문자열을 입력받습니다.
s = input()

# 문자열을 리스트로 변환 (수정이 가능하도록)
arr = list(s)

# 앞에서 2번째 원소 (인덱스 1)를 'a'로 변경
arr[1] = 'a'

# 뒤에서 2번째 원소 (인덱스 -2)를 'a'로 변경
arr[-2] = 'a'

# 리스트를 다시 문자열로 합쳐서 출력
result = "".join(arr)
print(result)